{
  "name": "todo-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">= 18"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon app.js",
    "build": "npm install && rimraf ./build && tsc",
    "start": "node build/app.js"
  },
  "author": "Pratik",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^5.1.0",
    "mongoose": "^6.10.0",
    "morgan": "^1.10.0",
    "rimraf": "^4.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
